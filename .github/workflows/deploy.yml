name: Deploy Services

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      reset_db:
        description: 'Reset database (prisma db push)'
        required: false
        default: 'false'
        type: boolean

jobs:
  deploy:
    name: Deploy to Hetzner
    runs-on: ubuntu-latest

    steps:
      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.HETZNER_SSH_KEY }}" > ~/.ssh/hetzner
          chmod 600 ~/.ssh/hetzner
          cat >> ~/.ssh/config << EOF
          Host services
            HostName ${{ secrets.HETZNER_SERVICES_IP }}
            User root
            IdentityFile ~/.ssh/hetzner
            StrictHostKeyChecking no
          EOF

      - name: Pull latest code
        run: |
          ssh services << 'SCRIPT'
          cd /opt/ottochain-services
          git fetch origin main
          git reset --hard origin/main
          echo "Pulled: $(git log --oneline -1)"
          SCRIPT

      - name: Install dependencies
        run: |
          ssh services << 'SCRIPT'
          cd /opt/ottochain-services
          pnpm install --frozen-lockfile || pnpm install
          SCRIPT

      - name: Build
        run: |
          ssh services << 'SCRIPT'
          cd /opt/ottochain-services
          pnpm run build
          SCRIPT

      - name: Apply schema changes
        run: |
          ssh services << 'SCRIPT'
          cd /opt/ottochain-services
          # Always sync schema with db push (dev-friendly, no migration files needed)
          npx prisma db push
          SCRIPT

      - name: Reset database (full wipe)
        if: ${{ github.event.inputs.reset_db == 'true' }}
        run: |
          ssh services << 'SCRIPT'
          cd /opt/ottochain-services
          # Drop all data and recreate schema
          npx prisma db push --force-reset --accept-data-loss
          SCRIPT

      - name: Restart services
        run: |
          ssh services << 'SCRIPT'
          cd /opt/ottochain-services
          pm2 restart all
          pm2 save
          sleep 5
          pm2 list
          SCRIPT

      - name: Health check
        run: |
          sleep 5
          
          EXPLORER=$(ssh services 'curl -sf -o /dev/null -w "%{http_code}" http://localhost:8080' || echo "000")
          INDEXER=$(ssh services 'curl -sf http://localhost:3031/health | jq -r .status' || echo "down")
          BRIDGE=$(ssh services 'curl -sf -o /dev/null -w "%{http_code}" http://localhost:3030/health' || echo "000")
          GATEWAY=$(ssh services 'curl -sf -o /dev/null -w "%{http_code}" http://localhost:4000/graphql' || echo "000")

          echo "Explorer: HTTP $EXPLORER"
          echo "Indexer: $INDEXER"
          echo "Bridge: HTTP $BRIDGE"
          echo "Gateway: HTTP $GATEWAY"

          echo "## 🚀 Services Deployed" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Service | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|---------|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| Explorer | HTTP $EXPLORER |" >> $GITHUB_STEP_SUMMARY
          echo "| Indexer | $INDEXER |" >> $GITHUB_STEP_SUMMARY
          echo "| Bridge | HTTP $BRIDGE |" >> $GITHUB_STEP_SUMMARY
          echo "| Gateway | HTTP $GATEWAY |" >> $GITHUB_STEP_SUMMARY

      - name: Update release/scratch branch
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Force push to release/scratch
        run: |
          git push origin main:release/scratch --force
          echo "## 📌 Release Branch Updated" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Pushed \`main\` → \`release/scratch\` at $(git rev-parse --short HEAD)" >> $GITHUB_STEP_SUMMARY
